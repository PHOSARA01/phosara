# PHOSARA_Website_UI_System_v1.0

Blueprint・Copy・Design System・Component Libraryを基準とするUI仕様書。

## 01 レイアウトシステム

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 02 ブレークポイント

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 03 ページテンプレート

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 04 TOP UI

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 05 ABOUT UI

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 06 APPROACH UI

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 07 SERVICES UI

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 08 INDUSTRIES UI

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 09 WORKS UI

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 10 JOURNAL UI

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 11 CONTACT UI

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 12 ナビゲーション動作

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 13 スクロール挙動

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 14 アニメーション仕様

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 15 フォーカス管理

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 16 エラーパターン

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。

## 17 UI運用ルール

### 目的

UIの一貫性を維持する。 \### 設計方針 BlueprintおよびWebsite
Copyに従い、Design SystemとComponent Libraryを利用する。 \### 実装要件
Mobile First・WCAG配慮・再利用可能なコンポーネント構成。
