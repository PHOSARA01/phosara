# PHOSARA_Website_Component_Library_v1.0

Website Blueprint・Website Copy・Website Design
Systemを基準とする公式コンポーネントライブラリ。

## 01 Header

### 目的

Headerの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 02 Global Navigation

### 目的

Global Navigationの役割を担う。 \### 使用場所 該当ページ共通 \###
構成要素 見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 03 Hero

### 目的

Heroの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 04 Section Header

### 目的

Section Headerの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 05 CTA

### 目的

CTAの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 06 Primary Button

### 目的

Primary Buttonの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 07 Secondary Button

### 目的

Secondary Buttonの役割を担う。 \### 使用場所 該当ページ共通 \###
構成要素 見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 08 Card

### 目的

Cardの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 09 Service Card

### 目的

Service Cardの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 10 Industry Card

### 目的

Industry Cardの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 11 Work Card

### 目的

Work Cardの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 12 Journal Card

### 目的

Journal Cardの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 13 Contact Form

### 目的

Contact Formの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 14 Footer

### 目的

Footerの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 15 Breadcrumb

### 目的

Breadcrumbの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 16 Pagination

### 目的

Paginationの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 17 Modal

### 目的

Modalの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 18 Loading

### 目的

Loadingの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 19 Empty State

### 目的

Empty Stateの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 20 Error State

### 目的

Error Stateの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 21 Cookie Banner

### 目的

Cookie Bannerの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 22 Search Box

### 目的

Search Boxの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 23 FAQ Accordion

### 目的

FAQ Accordionの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 24 Timeline

### 目的

Timelineの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 25 Statistics Block

### 目的

Statistics Blockの役割を担う。 \### 使用場所 該当ページ共通 \###
構成要素 見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 26 Testimonial

### 目的

Testimonialの役割を担う。 \### 使用場所 該当ページ共通 \### 構成要素
見出し・本文・必要要素 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ
コントラスト・キーボード操作・代替テキスト対応 \### 実装時の注意点
再利用可能なコンポーネントとして実装する。

## 27 Component Rules

### 目的

全コンポーネントの運用基準を定義する。 \### 使用場所 サイト全体 \###
構成要素 命名規則・再利用・保守 \### サイズ Design System準拠 \### 余白
8pxグリッド準拠 \### タイポグラフィ Design System準拠 \### カラー
ブランドカラー準拠 \### 状態 Default / Hover / Active / Focus / Disabled
\### レスポンシブ Mobile First \### アクセシビリティ WCAG準拠 \###
実装時の注意点 Blueprint・Copy・Design Systemを変更せず実装する。
